import { SearchDataFilterPipe } from './search-data.pipe';

describe('SearchDataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
